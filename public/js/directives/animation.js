/**
 * @name: animate.js
 * @description: Main file to write the Java Script AngularJS animations.
 * Basicaly animations won't require this file because most of them are
 * based on ng-show/hide tags, something special may be written here.
 */

angular.module('app').directive('animateTest', function($animate){
   return function(scope, elem, attr){
    return;
  };  
});